@using ApplicationCore.ServiceInterface
@inject IGenreService _genreService


@foreach (var genre in await _genreService.GetGenreList())
{
    @*@Html.ActionLink(genre.Name, "Genre", "Movies", new { genreId = genre.Id }, new { @class = "dropdown-item" })*@

    <a asp-action="Genre" asp-controller="Home" class="dropdown-item" asp-route-gid="@genre.Id"> @genre.Name</a>
}
@model List<ApplicationCore.Models.MovieCardResponseModel>

@{
    ViewData["Title"] = "Home Page";
}

@*partial Views...shared folder, file name should be _viewname

have same view: same datatype but different data
HomeController => Index action Method => List<movieCard>1. home page view (index) foreach => moivecard=> moviecard
2. purchase movie  moviecard
3. favorite movies moviecard*@

<div class="rounded">
    <div class="container-fluid bg-light">
        <div class="row">
            @foreach (var movie in Model)
            {
                <div class="col-2">
                    <partial name="_MovieCard" model="movie"/>
                    @*@await Html.PartialAsync(partialViewName:"_MovieCard",movie)*@
                </div>
            }
        </div>
    </div>
</div>
